#include <stdio.h>
float itr(int x, int y);
int main()
{

    return 0;
}
float itr(int x, int y)
{
    float a, b, c, d;
    printf("Enter the value whose square roots has to be found --> ")
        scanf("%d", &b);
    int G = sqrt(b);
    do
    {
        a = (G, G + 1) / 2;
        
    } while (int J = 0)
     return 0;
}