#include <stdio.h>
int main()
{
    printf("Enter the height of -->");

    for (int d = 0; d < ; d++)
    {
        for (int e = 0; e < d; e++)
        {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}